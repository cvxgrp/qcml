dimension n = 1000  # dimension of problem
dimension m = 30

# testing comments
variable x(n)

parameter mut(1,n)
parameter B
parameter gamma positive
parameter Ft(m,n)
parameter D(n,n)

# ignoring comments?
maximize mut*x - gamma*(square(norm(Ft*x)) + square(norm(D*x)))
subject to
    sum(x) == B # ignored?
    x >= 0