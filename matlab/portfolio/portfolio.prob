dimension n = 1000
dimension m = 30

variable x(n)

parameter mut(1,n)
parameter B
parameter gamma positive
parameter Ft(m,n)
parameter D(n,n)

maximize mut*x - gamma*(square(norm(Ft*x)) + square(norm(D*x)))
subject to
    sum(x) == B
    x >= 0